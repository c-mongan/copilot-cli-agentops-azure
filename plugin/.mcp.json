{
  "mcpServers": {
    "azure-mcp": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@azure/mcp@latest",
        "server",
        "start",
        "--read-only",
        "--namespace",
        "monitor"
      ]
    }
  }
}
